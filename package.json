{
  "name": "quotes-api",
  "version": "1.0.0",
  "private": true,
  "description": "A tiny quotes API with a minimal web page, shipped through a Docker + GitHub Actions pipeline.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
